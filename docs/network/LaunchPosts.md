# Launch Posts — Final

## Show HN

**Title:**
```
Show HN: Agent Arbiter — Random AI Task Assignment You Can Verify
```

**Body:**
```
Hi HN,

I built a small demo called Agent Arbiter.

The demo assigns tasks to AI agents.

The assignment itself is intentionally simple:

  3 agents
  5 tasks
  random assignment

The question I wanted to explore wasn't:

  "How do we assign tasks?"

It was:

  "How do we prove the assignment was fair?"

Today most systems use Math.random().

That works fine operationally, but it doesn't leave much of an
audit trail.

With Agent Arbiter, every assignment uses a signed Verifiable
Entropy Object (VEO-1) generated by OpenRNG.

Each assignment includes:

  - entropy source information
  - entropy confidence score
  - cryptographic signature
  - optional blockchain anchor
  - independent verification

The assignment result is identical.
The proof is different.

Example:

  With Math.random():
  "Agent-beta was assigned. Trust me."

  With VEO-1:
  "Agent-beta was assigned.
   Entropy from drand + Bitcoin + Polygon.
   ECS: 871 (AA).
   Signed by 0xD4F7…
   Verify it yourself."

The demo includes an audit mode that replays every assignment
and verifies every entropy object.

Run it:
  git clone https://github.com/ned-del/openrng.git
  cd openrng/examples/agent-arbiter
  npx ts-node arbiter.ts --audit

Verify any object in your browser:
  https://verify.openrng.io

Repo: https://github.com/ned-del/openrng
Whitepaper: https://github.com/ned-del/openrng/blob/main/docs/whitepaper/OpenRNG_Whitepaper_v1.md

I'm interested in feedback on two questions:

1. Are there real situations where random decisions should be
   independently auditable?

2. If you're building AI agents, simulations, games, lotteries,
   validator selection systems, etc., would this be useful?

Happy to answer questions.
```

### Prepared counterargument responses (keep in pocket for comments)

**"Why not just use Math.random()?"**

For most things, Math.random() is fine. You don't need verifiable entropy to shuffle a playlist. But when a random decision has consequences — an AI agent choosing which tool to call, a game determining a payout, a simulation producing a published result — you need to answer "was this fair?" after the fact. Math.random() can't answer that because it doesn't record where the randomness came from. A VEO can.

**"Why not Chainlink VRF?"**

Chainlink VRF proves that a specific output was derived from a specific seed and key. That's useful on-chain. But VRF proves derivation, not entropy quality. It uses a single source, doesn't score confidence, doesn't carry provenance metadata, and requires an on-chain callback. VEO wraps multiple sources, scores them, signs the result, and works off-chain with optional anchoring. They solve different problems. VRF is a cryptographic primitive. VEO is an infrastructure layer.

**"Why not QRNG?"**

Quantum RNGs produce excellent entropy. But QRNG services return raw numbers with no provenance, no scoring, and no verification. You have to trust the QRNG provider. OpenRNG could use a QRNG as one of its entropy sources — we'd welcome it. The point isn't the source; it's the verifiable container around it.

**"Why not use drand directly?"**

We do use drand — it's one of our three sources. drand is great: public, verifiable, threshold-BLS. But drand is a single source. If you use only drand, your diversity score is low, and a drand outage means you have no entropy. OpenRNG aggregates drand with Bitcoin and Polygon block hashes, scores the combination, and signs the result. drand is an ingredient. VEO is the product.

**"Why would AI agents need this?"**

Today, when an agent randomly selects a tool, samples a response, or routes a task, nobody can verify the decision was fair. The agent's operator can't. The user can't. A regulator can't. This is fine for chatbots. It's not fine for agents managing money, making medical triage decisions, or operating autonomously. As agents get more authority, their random decisions need audit trails.

**"This is overengineered."**

Maybe. That's what I'm trying to find out. If the answer is "nobody needs auditable randomness," that's useful to know. But every time I talk to people building regulated systems — gaming, finance, compliance — they ask how to prove their randomness was fair. This is an answer to that question.

**"What's the latency?"**

~200-500ms per VEO (network + 3 source fetches). If you need sub-millisecond, batch VEOs and consume from a local pool. The verification is instant — client-side SHA-256 + signature recovery.

---

## Twitter/X Thread

```
1/ I built a small demo called Agent Arbiter.

3 AI agents. 5 tasks. Random assignment.

The question isn't how to assign tasks.
It's how to prove the assignment was fair.

git clone https://github.com/ned-del/openrng
cd openrng/examples/agent-arbiter
npx ts-node arbiter.ts --audit

2/ Each assignment uses a signed Verifiable Entropy Object
from 3 independent sources:

  • drand randomness beacon
  • Bitcoin latest block hash
  • Polygon latest block hash

Not Math.random(). Signed, scored, verifiable.

3/ --audit replays every assignment.

Re-derives which agent should have been assigned.
Cryptographically verifies every entropy object.

  Re-derived: agent-gamma ✓ MATCH
  Hash: ✓ | Signature: ✓ | Sources: ✓

4/ Each object carries an Entropy Confidence Score (0–1000):

  871 — Grade AA
  3 live sources, 0 fallbacks

Not all randomness is equal. ECS tells you how
trustworthy yours is.

5/ With Math.random():
  "Agent-beta was assigned. Trust me."

With VEO-1:
  "Agent-beta was assigned. Here's the proof."

The assignment is the same.
The proof is the difference.

6/ Verify any object in your browser:

  verify.openrng.io

Client-side signature recovery. Zero trust required.

No token. No DAO. Frozen protocol. 74 tests passing.

github.com/ned-del/openrng
```

---

## LinkedIn

```
Every AI agent that makes a random decision has a trust problem.

Which tool to use? Which task to take? Which response to sample?

The answer is usually Math.random().
The proof is usually "trust me."

I built a small demo called Agent Arbiter to explore what
changes when random decisions come with cryptographic proof.

3 agents, 5 tasks. Each assignment backed by a Verifiable
Entropy Object with source provenance, quality scoring,
and a signature anyone can verify.

The audit mode replays every assignment and verifies every
entropy object. If anyone claims the assignment was unfair,
the proof says otherwise.

The assignment is the same. The proof is the difference.

  git clone https://github.com/ned-del/openrng
  cd openrng/examples/agent-arbiter
  npx ts-node arbiter.ts --audit

  Verify: verify.openrng.io

I'm exploring whether verifiable entropy is useful
infrastructure for AI agents, games, simulations, and
other systems where random decisions need audit trails.

No token. No speculation. Just a frozen protocol and a
working demo.
```
